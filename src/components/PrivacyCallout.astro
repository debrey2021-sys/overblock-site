<section class="privacy" id="privacy">
  <div class="privacy-inner fade-up">
    <p class="eyebrow">Privacy</p>
    <h2>Designed around your privacy.</h2>
    <p class="body">
      No account required. No data collected. Your pay information stays on your device and is never transmitted anywhere.
    </p>
    <ul class="points" aria-label="Privacy highlights">
      <li>No account</li>
      <li>No cloud</li>
      <li>No tracking</li>
      <li>No subscription</li>
    </ul>
  </div>
</section>

<style>
  .privacy {
    padding: var(--section-pad) 24px;
    background: var(--color-dark);
  }

  .privacy-inner {
    max-width: 760px;
    margin: 0 auto;
    text-align: center;
  }

  .eyebrow {
    color: var(--color-accent);
    font-size: 0.84rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h2 {
    margin-top: 18px;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1.06;
    color: var(--color-dark-text);
  }

  .body {
    max-width: 620px;
    margin: 20px auto 0;
    color: var(--color-dark-muted);
    font-size: 1.06rem;
    line-height: 1.75;
  }

  .points {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 18px;
    margin-top: 32px;
    list-style: none;
  }

  .points li {
    color: var(--color-accent);
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 0.03em;
  }
</style>
